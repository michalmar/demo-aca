# Cosmos DB Emulator / Azure configuration
# When using local emulator in Docker:
#   docker run -p 8081:8081 -p 10251:10251 -p 10252:10252 -p 10253:10253 -p 10254:10254 \
#     --name cosmos-emulator -e AZURE_COSMOS_EMULATOR_ENABLE_DATA_PERSISTENCE=true \
#     mcr.microsoft.com/cosmosdb/linux/azure-cosmos-emulator:latest
# Endpoint: https://localhost:8081/
# Obtain the primary key via emulator logs or Azure Portal when deployed.

COSMOS_ENDPOINT=https://localhost:8081/
COSMOS_KEY=YOUR_EMULATOR_PRIMARY_KEY
COSMOS_DB_NAME=questionnaire_db
COSMOS_CONTAINER=answers
